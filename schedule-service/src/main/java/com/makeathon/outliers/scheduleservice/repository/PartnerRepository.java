package com.makeathon.outliers.scheduleservice.repository;

import com.makeathon.outliers.scheduleservice.entity.PartnerEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PartnerRepository extends JpaRepository<PartnerEntity, Integer> {
}
